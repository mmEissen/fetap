from setuptools import setup, find_packages

setup(
    name='fetap',
    version='0.1.0',
    author='Ignore',
    author_email='author@gmail.com',
    description='',
    packages=find_packages(),
    install_requires=[],
)